---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: alertsources.argonix.io
spec:
  group: argonix.io
  names:
    kind: AlertSource
    listKind: AlertSourceList
    plural: alertsources
    singular: alertsource
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.sourceType
      name: Source
      type: string
    - jsonPath: .spec.isActive
      name: Active
      type: boolean
    - jsonPath: .status.totalReceived
      name: Received
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AlertSource is the Schema for the alertsources API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AlertSourceSpec defines the desired state of an AlertSource.
            properties:
              autoInvestigate:
                default: false
                description: AutoInvestigate enables Argos AI auto-investigation for
                  ingested alerts.
                type: boolean
              autoRemediate:
                default: false
                description: AutoRemediate enables Argos AI auto-remediation for ingested
                  alerts.
                type: boolean
              channels:
                description: Channels is a list of alert channel UUIDs to notify when
                  alerts are ingested.
                items:
                  type: string
                type: array
              connector:
                description: Connector is the UUID of the connector to use for investigation/remediation.
                type: string
              filters:
                description: Filters is a JSON-encoded filter configuration for incoming
                  alerts.
                type: string
              isActive:
                default: true
                description: IsActive indicates whether the alert source is enabled.
                type: boolean
              name:
                description: Name is the display name of the alert source.
                type: string
              sourceType:
                description: SourceType is the type of external alerting system.
                enum:
                - alertmanager
                - datadog
                - grafana
                - pagerduty
                - opsgenie
                - generic
                type: string
            required:
            - name
            - sourceType
            type: object
          status:
            description: AlertSourceStatus defines the observed state of an AlertSource.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the resource's state.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              dateCreated:
                description: DateCreated is the creation timestamp.
                type: string
              dateModified:
                description: DateModified is the last modification timestamp.
                type: string
              id:
                description: ID is the UUID of the alert source in the Argonix API.
                type: string
              lastReceivedAt:
                description: LastReceivedAt is the timestamp of the last received
                  alert.
                type: string
              totalReceived:
                description: TotalReceived is the total number of alerts received.
                format: int64
                type: integer
              webhookSecret:
                description: WebhookSecret is the secret token for the webhook endpoint.
                type: string
              webhookUrl:
                description: WebhookURL is the full webhook URL for sending alerts
                  to this source.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
